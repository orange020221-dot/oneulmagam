# 실습 3. 배포와 운영 환경

과목: 신인류AI사피엔스경험디자인 CHS7006
작성: 이재훈 2021314509

수업 순서: GitHub → Vercel 배포 → Supabase DB → GA 측정 → 서치콘솔.

---

## 이 서비스에서 측정하는 것

강의의 프로포즈 앱은 “홀 추천 클릭”이 KPI였다. 오늘마감의 가설은 **매일 아이캠퍼스를 안 들어가도 마감을 안 놓치는가**다. 그래서 설문 대신 아래만 쌓는다.

| 이벤트 | 의미 |
|---|---|
| visit | 화면을 열었다 |
| add_item | 일정을 직접 넣었다 |
| complete_item | 오늘 할 일을 처리했다 |
| today_cleared | 오늘 남은 마감이 0이 됐다 |
| share_click | 같은 과 친구에게 링크를 복사했다 |

키는 HTML에 넣지 않는다. Vercel 환경변수만 본다.

---

## 1. GitHub

저장소: https://github.com/orange020221-dot/oneulmagam

## 2. Vercel

공개 주소: **https://oneulmagam.vercel.app**

정적 페이지 + `/api/event`, `/api/config`.
커밋할 때마다 자동 배포하려면 Vercel 계정에 GitHub Login Connection을 추가한 뒤 저장소를 연결한다.

환경변수

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `GA_MEASUREMENT_ID`
- `SITE_URL`

## 3. Supabase

1. supabase.com → New project → Personal / Free / Seoul
2. SQL Editor에 `supabase/setup.sql` 붙여넣고 Run
3. Settings → API에서 Project URL, anon public key 복사
4. Vercel Environment Variables에 넣고 Redeploy

테이블 `kpi_events`는 익명 insert만 허용한다.

## 4. Google Analytics

1. analytics.google.com → 측정 시작
2. 계정/속성 이름: 오늘마감
3. 시간대 대한민국, 플랫폼 웹, URL은 Vercel 주소
4. 측정 ID `G-...` 를 `GA_MEASUREMENT_ID`에 넣는다
5. 실시간 보고서에서 방문이 잡히는지 확인
6. `share_click`, `today_cleared` 이벤트가 KPI다

## 5. Search Console

1. search.google.com/search-console
2. URL 접두어로 Vercel 주소 추가
3. GA가 있으면 소유권 확인이 된다
4. 사이트맵 `sitemap.xml` 제출
5. URL 검사 후 색인 요청

검색 키워드는 범용어가 아니라 **오늘마감**.
